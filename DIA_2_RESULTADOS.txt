================================================================================
DÍA 2 — RESOLVER NAVEGACIÓN: LINK "INICIO" + REETIQUETADOS + CLASSROOM CLARITY
CONSULTING MODE (Admin + Centro Operativo) — RESULTADOS
Fecha: 12 de junio de 2026
================================================================================

MÉTODO USADO
------------
- Revisión de diff (git diff) de los archivos modificados antes de validar,
  para confirmar qué se cambió realmente frente a lo planeado.
- Servidor Flask local levantado (python app.py, puerto 5000, BRAND por
  defecto = consulting) con la BD de producción (data/cog_work_log.db).
- Login real como admin (admin/admin123) vía /api/auth/login (con token
  CSRF obtenido de la página de login) y verificación de HTTP status +
  HTML renderizado de /mod/dashboard y /admin/menu.
- Validación de roles staff (jefe_estacion, operador, auditor) en un
  entorno AISLADO con BD temporal (scripts/tests/fixtures.py:
  make_test_env + seed_baseline), sin tocar la BD de producción ni
  necesitar contraseñas reales — mismo criterio de Día 1.
- Cambio de brand a Petroleum sobre la sesión admin (vía /api/set-brand)
  para confirmar que el Cambio 2 quedó aislado a Consulting.


HALLAZGO INICIAL — LOS 3 CAMBIOS YA ESTABAN EN EL WORKING TREE
---------------------------------------------------------------
Al iniciar el Día 2, `git status` ya mostraba templates/layout.html y
templates/admin_menu.html como modificados (sin commit). La revisión del
diff confirmó que los 3 cambios del plan YA estaban implementados, con un
enfoque más simple/limpio que el descrito originalmente:

- CAMBIO 1 (link "Inicio"): implementado 100% server-side con
  `{% if session.get('role') == 'admin' %}` en layout.html. NO requirió
  tocar static/js/common.js (el plan asumía que Jinja2 no tenía acceso al
  rol, pero `session.get('role')` SÍ está disponible).
- CAMBIO 3 (mover "Módulos SGM/SASISOPA"): en vez de duplicar el link como
  sugería el plan, se ELIMINÓ la línea redundante de "Control principal"
  y se reutilizó el nuevo link "Inicio" del Cambio 1 (ya al tope, visible
  sin abrir subsecciones).
- CAMBIO 2 (Bitácora -> Calendario operativo): en vez de renombrar la línea
  "Bitácora" (-> /mod/activities), se ELIMINÓ esa línea duplicada y se
  renombró la línea "Calendario" existente (-> /mod/operational-calendar,
  el destino real) a "Calendario operativo".

El trabajo de Día 2 se centró en REVISAR y VALIDAR estos cambios ya
escritos, no en reescribirlos desde cero.


REVISIÓN DE CÓDIGO (DIFF)
--------------------------

1) templates/layout.html — líneas ~52-58 ("Acceso rápido")
   ANTES:
     <a href="/mod/dashboard">Dashboard</a>
   DESPUÉS:
     {% if session.get('role') == 'admin' %}
     <a href="/admin/menu">{{ "Inicio · Centro de módulos Petroleum" if
       session.get('brand','consulting') == 'petroleum' else
       "Inicio · Centro de módulos" }}</a>
     {% else %}
     <a href="/staff/menu">Inicio</a>
     {% endif %}
     <a href="/mod/dashboard">Resumen / KPIs</a>
   -> Link "Inicio" como PRIMER elemento de "Acceso rápido", visible en
      TODAS las páginas con layout.html (sección sin data-admin-only).
      "Dashboard" se conserva, renombrado a "Resumen / KPIs".

2) templates/layout.html — sección "Administrador" > "Control principal"
   ANTES (línea ~111):
     <a href="/admin/menu">{{ "Centro de módulos Petroleum" if ... else
       "Módulos (SGM / SASISOPA)" }}</a>
   DESPUÉS:
     (línea eliminada)
   -> Sin duplicado visual: el acceso a /admin/menu ya vive en "Acceso
      rápido" (Cambio 1), siempre visible sin abrir "Control principal".

3) templates/admin_menu.html — toolbox "Operación diaria" (rama
   {% else %} = Consulting, dentro de {% if brand=='petroleum' %}...
   {% else %}...{% endif %})
   ANTES:
     <a href="/mod/activities">Bitácora</a>
     <a href="/mod/panel">Mi panel</a>
     <a href="/mod/operational-calendar">Calendario</a>
   DESPUÉS:
     <a href="/mod/panel">Mi panel</a>
     <a href="/mod/operational-calendar">Calendario operativo</a>
   -> Confirmado: la rama Petroleum ({% if brand=='petroleum' %}) tiene su
      PROPIA card "Operación diaria" con "Calendario" (sin "operativo"),
      NO se tocó. El cambio queda aislado a Consulting, como pedía la
      NOTA IMPORTANTE del plan.

static/js/common.js: SIN CAMBIOS (confirmado y correcto — no se necesita
lógica JS adicional con el enfoque server-side).


VALIDACIÓN EN VIVO — RESULTADOS
---------------------------------

PASO A: Servidor Flask
  - Arrancó sin errores en http://127.0.0.1:5000 (modo debug off).

PASO B: Cambio 1 — Link "Inicio" por rol/brand
  Probado en /mod/dashboard (página con sidebar completo):

  | Usuario        | Rol           | Brand      | Label renderizado                          | href          | Status |
  |----------------|---------------|------------|---------------------------------------------|---------------|--------|
  | admin          | admin         | consulting | "Inicio · Centro de módulos"                | /admin/menu   | 200 OK |
  | admin          | admin         | petroleum  | "Inicio · Centro de módulos Petroleum"      | /admin/menu   | 200 OK |
  | jefe_test      | jefe_estacion | consulting | "Inicio"                                     | /staff/menu   | 200 OK |
  | operador_test  | operador      | consulting | "Inicio"                                     | /staff/menu   | 200 OK |
  | auditor_test   | auditor       | consulting | "Inicio"                                     | /staff/menu   | 200 OK |

  - El link aparece SIEMPRE como primer elemento de "Acceso rápido", en
    CUALQUIER página que extienda layout.html (no solo /mod/dashboard).
  - /admin/menu para admin -> 200. /staff/menu para admin -> 403 (correcto:
    @role_required excluye admin de /staff/menu, y admin no necesita esa
    ruta porque su "Inicio" apunta a /admin/menu).
  - /staff/menu para jefe_estacion/operador/auditor -> 200 (coincide con
    @role_required("operador","jefe_estacion","contador","auditor")).

PASO C: Cambio 2 — "Bitácora" -> "Calendario operativo"
  - /admin/menu (Consulting): toolbox "Operación diaria" ahora muestra
    "Calendario operativo" -> /mod/operational-calendar (200 OK). El link
    "Bitácora" ya no existe (sin duplicado, sin label engañoso).
  - /admin/menu (Petroleum, misma sesión admin con brand cambiado vía
    /api/set-brand): su propia card "Operación diaria" conserva "Mi panel",
    "Calendario" (sin "operativo"), "Evidencias", "Organigrama",
    "Notificaciones" — INTACTA, sin efectos del cambio de Consulting.

PASO D: Cambio 3 — Sidebar admin reorganizado
  - /mod/dashboard, sección "Administrador" > "Control principal": ya NO
    tiene el link a /admin/menu. Solo quedan Inbox (Revisión), Estaciones,
    Usuarios (+ links Petroleum si aplica). Sin duplicado visual.
  - El acceso a /admin/menu para admin queda únicamente en "Acceso rápido"
    (Cambio 1), al tope del sidebar, sin necesidad de abrir subsecciones.

PASO E: Regresiones
  - Todas las rutas tocadas devuelven 200/403 según corresponde — sin
    404/500.
  - data-admin-only y demás atributos de visibilidad por rol siguen
    presentes en el HTML crudo (la ocultación la aplica common.js en el
    navegador vía loadMe(), sin cambios en esa lógica).
  - Jinja2 balanceado: {% if %}/{% else %}/{% endif %} de brand y de rol
    cierran correctamente; no se rompió la estructura de "Operación diaria"
    Petroleum vs Consulting.


CHECKLIST DE COMPLETITUD
=========================

Cambios de código:
  [x] Línea "Inicio" insertada en layout.html (Acceso rápido, dinámica por
      rol y brand)
  [x] "Bitácora" renombrado/consolidado en "Calendario operativo" en
      admin_menu.html (solo Consulting)
  [x] Link admin reorganizado en layout.html (tope de "Acceso rápido")
  [x] Link duplicado removido de "Control principal"
  [ ] JS dinámico en common.js -> NO APLICA (enfoque server-side cubre el
      objetivo sin tocar JS)

Validación:
  [x] Servidor reiniciado sin errores
  [x] Link "Inicio" aparece en páginas con sidebar (layout.html), para
      todos los roles probados (admin, jefe_estacion, operador, auditor)
  [x] Link "Inicio" apunta correctamente según rol (admin -> /admin/menu,
      resto -> /staff/menu) y según brand (Petroleum suffix solo admin+
      petroleum)
  [x] Botón "Calendario operativo" en toolbox de /admin/menu (Consulting)
      apunta a /mod/operational-calendar
  [x] Rama Petroleum del toolbox NO afectada (sigue con "Calendario")
  [x] Roles/visibilidades (data-admin-only, etc.) sin cambios
  [x] Sin 404/500 en las rutas probadas

Documentación:
  [x] DIA_2_RESULTADOS.txt creado con findings de revisión y validación


CONCLUSIÓN
===========
Los 3 cambios del Día 2 están implementados y validados en vivo:
- "Inicio" siempre visible y funcional desde cualquier pantalla con
  sidebar, apuntando al hub correcto según rol (admin -> /admin/menu,
  staff -> /staff/menu) y brand (Petroleum/Consulting).
- El toolbox de /admin/menu (Consulting) ya no usa la etiqueta engañosa
  "Bitácora"; "Calendario operativo" coincide con su destino real.
- El sidebar admin tiene acceso directo a "Inicio / Centro de módulos" sin
  abrir subsecciones, sin links duplicados.

No se detectaron regresiones. Las páginas data/cog_work_log.db,
data/cog_work_log.db-shm y data/cog_work_log.db-wal que aparecen en
git status son preexistentes a esta sesión (no generadas por esta
validación).

Próximo: Día 3 - Breadcrumbs y "volver a" consistentes en páginas de
2do nivel.

================================================================================
FIN DE RESULTADOS — DÍA 2
================================================================================
